[package]
name = "receipts-orchestration"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.1"
publish = false

[lib]
path = "../../src/core/lib.rs"

[dependencies]
receipts-workspace-execution = { path = "../receipts-workspace-execution" }
